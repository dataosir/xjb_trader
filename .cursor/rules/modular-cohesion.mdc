---
description: 高内聚低耦合与文件体量上限；禁止单文件无限膨胀（含 selftest 新增用例）
alwaysApply: true
---

# 模块化：高内聚、低耦合

参考 Java 包/类职责划分：**一个模块只做一件事，模块间通过窄接口协作**。

## 文件体量（新代码强制）

| 类型 | 软上限 | 硬上限 | 超限处理 |
|------|--------|--------|----------|
| 单 `.py` 文件 | **500 行** | **800 行** | 必须拆文件；禁止在同一文件继续堆逻辑 |
| 单函数 | — | **~80 行** | 抽子函数或下沉到职责单一的模块 |

**例外（只读、不继续膨胀）**：`tea/selftest.py`（~4000 行历史单体）在专项拆分任务前保持不动，但**禁止在其中大块追加新用例**（见下）。

## selftest 新增用例（强制）

1. **新断言** → `tea/tests/checks/<域>.py`（如 `screening.py`、`portfolio.py`），按被测子包对齐。
2. `tea/selftest.py` **只做编排**：import `check_*`、注册进 `Suite`、保留 `main()` 入口与沙箱初始化。
3. 改已有公式/阈值时，优先改对应 `checks/` 文件；仅在遗留块内小改可暂留 `selftest.py`。
4. 禁止为通过 CI 而把断言改成「引擎当前输出」快照。

## 内聚与耦合

- **高内聚**：同一文件内的函数/类服务于同一领域（如 `gates.py` 只管门禁）。
- **低耦合**：跨域只通过 `__init__.py` 导出或上层编排调用；禁止循环 import。
- **依赖单向**：遵守 `docs/tech/00-engineering-standards.md` 分层（`runtime → … → core`）。
- **CLI 无业务**：`cli.py` 解析与展示；逻辑在 `runner` / 子包。
- **配置集中**：阈值进 `config_store.DEFAULTS`，不散落魔法数。

## 触达大文件时

修改 `screener.py`、`cli.py`、`runner.py` 等超标遗留文件时：**只改任务相关最小范围**；若新增 >50 行，优先抽到同层新模块而非继续加长原文件。

## 提交前

`python -m tea selftest` 全绿 + `ruff check .` 无告警。
