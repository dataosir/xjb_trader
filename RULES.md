# 🛠️ 技术迭代铁律与规约 (Technical Rules & Constraints)

> **权威源**：本文件。配套阅读：[`docs/README.md`](docs/README.md)（4 步闭环）、[`docs/tech/00-engineering-standards.md`](docs/tech/00-engineering-standards.md)（工程细节）。  
> 本规约适用于后续所有由 AI 辅助的技术实现、代码重构及架构变更。AI 必须严格遵守以下约束，**严禁擅自扩展、猜测或引入未被明确批准的技术栈**。

---

## 一、 核心技术栈铁律（绝对红线）

1. **禁止擅自引入新依赖**：
   - 严禁在未向我汇报并通过前，擅自引入新的第三方库、框架或改变现有依赖版本。
   - 必须优先使用项目当前已有的工具包和标准库。
2. **架构与分层边界**：
   - 严禁打破现有的项目架构分层。代码必须严格遵循各模块的既定职责，禁止跨层直接调用或产生循环依赖。
3. **命名与风格一致性**：
   - 严格遵循项目中现有的代码风格、命名规范（如驼峰、下划线、缩写习惯等）。严禁凭空创造新的命名规则。

---

## 二、 拒绝“AI 自由发挥”的实现原则

1. **保持代码极简（KISS 原则）**：
   - 严禁编写与当前任务无关的“炫技式”代码、超前设计或未被请求的抽象封装。
   - 只实现需求中明确指出的核心逻辑，不做过度设计。
2. **错误处理与防御性编程**：
   - 对核心逻辑（如 A 股交易准入、关键数据计算）必须显式处理异常，**严禁吞掉错误（禁止空 catch）**。
   - 遇到不确定的业务边界或风险点，**必须停止编码并先向我提问，严禁自行脑补业务逻辑**。
3. **日志与可观测性**：
   - 关键业务流程必须输出清晰、结构化的日志，便于后续通过 `logs/` 进行排查。

---

## 三、 技术文档与代码同步规范

1. **“代码未动，文档先行（或同步）”**：
   - 在动手修改代码前，必须先在 Plan 中明确受影响的技术模块。
   - 代码实现后，若涉及接口变化或数据结构调整，**必须同步更新 `docs/tech/` 下的对应文档**。
2. **禁止静默修改**：
   - 严禁在未同步更新 `CHANGELOG.md` 的情况下完成任务提交。
