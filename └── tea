或者大小写差异（`tea` 与 `TEA`），导致模块导入混乱或 Git 跟踪异常。

## 原因分析
1. **大小写冲突（最可能）**  
   Git 默认在文件系统大小写不敏感（如 macOS / Windows）上容易产生大小写冲突。  
   例如，曾经有 `TEA` 目录，后来更名为 `tea`，但 Git 历史中仍残留旧名称的 tree 对象，检出时可能同时出现新旧两个目录。

2. **Git 历史中存在重复 tree**  
   由于某些 merge 或 rebase 操作，在某一提交中包含了两个不同 `tree` 的 `tea` 目录（内容相同或不同），导致工作区出现双胞胎。

3. **未清理的打包产物**  
   如果打包脚本曾在根目录生成了 `tea/` 目录（文件拷贝），没有及时删除，会与源码目录并存。

4. **符号链接/ junction**  
   在 Windows 上如果使用 `mklink` 创建了链接，可能显示为另一个同名目录。

## 解决方案

### 步骤 1：确认真实结构
在项目根目录执行：
