# 变更记录

本文件格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复（2026-08-25 量价结构：缩量上涨不再给分）

> 可买 0/5 全是「今日硬拉但没后劲」的弱票（永杉锂业 量比 1.1、跌破 MA20），
> 而被拒的强势票反而赢。根因是量价结构「量价基本配合」给 1 分太松——只要
> 「涨 + 量比≥1.0」就给分，把缩量上涨的弱票也放进了可买。

- **缩量上涨(量比<1.2)且非多头 → 量价结构 0 分**（`preflight.score_nine`）：
  只有放量(≥1.2)+多头才 2 分、量比≥1.0+多头才 1 分，弱票不再靠「涨了今天」混分。
- **低吸（启动前夕）技术方案成文 TODO**：`docs/LOWBUY_PLAN_2026-08-25.md`——三阶段
  （前夕候选落盘 → 样本验证 → 上线低吸买入），数据先行、暂不买入。

### 改进（2026-08-25 数据缺口/网络异常在扫描结果里醒目提示）

> 指数取数超时、东财抖动这类数据缺口此前只在天气屏里一行 `! 数据缺口`，容易被忽略。
> 现在种子扫描结束时会打一条醒目的 ⚠️ 横幅，并写进 SEED 报告备注，复盘一眼可见。

- `sentiment.data_gap_summary` / `format_data_gap_banner`：汇总指数/板块/涨跌家数缺口
  与网络失败摘要（`网络请求 N 次｜东财 X｜失败 Z`），映射成中文标签。
- `runner.seed_plan`：扫描完成时若有缺口，打印 ⚠️ 横幅 + 写进 `result.notes` 随报告归档。

### 修复（2026-08-25 板块排序可交易性过滤 + 北交所涨停幅度）

> 08-25 种子扫描 EMPTY：TOP3 板块（化妆品/种子/清洁小家电）几乎全是创业板/北交所票，
> 用户只开主板，75 次「板块无权限」拒绝导致初筛 0。根因是两个逻辑缺口叠加。

- **板块排序只统计「可交易成员」**（`screener.rank_sectors`）：涨停家数、上涨比、温和票
  只统计 `board_allowed` 通过的主板成员，分母仍用全部成员数（可交易占比低的板块结构分自然偏低）。
  新增 `tradeable_n` 字段落盘留痕。
- **`limit_up_pct` 补北交所 30%**（`core.utils`）：原漏掉 `bse` 分支返回 10%，把北交所
  15.59% 涨幅误判成涨停（化妆品凭「假涨停」进了 TOP1）。现按 主板10% / 创业板科创板20% / 北交所30% / ST5%。

### 修复（2026-08-24 分时高位软否决改为「只对弱势票生效」）

> 招金黄金 08-21 分时 91%（龙头、放量+多头）被「分时高位」软否决，结果 T+1 +7.56%——
> 说明「分时位置」本身不是风险，强势票贴日内高是强势确认、弱势票贴高才是追高。

- **分时高位软否决加强势豁免**（`veto.check` + `veto.intraday_strong_exempt`，默认开）：
  放量上涨（chg>0 且 量比≥1.2 且 均线多头）的强势票，分时高位不再按「追高」否决；
  弱势票（缩量/均线失守/下跌）分时高位仍软否决。硬「分时封顶」0.95 与「涨停」否决保留。
- **留痕**：`veto.strong_exempt` 字段 + `scan_details` 候选明细，复盘可见「为何 91% 未被否决」。

### 策略（2026-08-24 胜率因子门槛阶段 A + 板块放宽通道排名封顶）

> 把实证胜率因素接入选票的第一阶段：不做加权（样本不够），先做「砍输家」的硬门槛。
> 依据 62 条回填样本（跨 T+1/T+3/T+5 单调）：板块排名 1-3 胜率 50%、6~15 仅 0~17%；
> 突破阶段仅 6%（T+3 -5.2%）。

- **胜率因子门槛（`screener._winrate_gate`）**：本可通过共振门槛的候选，若
  「板块排名 >5」或「突破阶段 + 排名 >3」，强制从可买降级观察轨（留痕 `winrate_gate`）。
  开关 `strategy.winrate_gate_enabled`，一键回滚。
- **板块「涨停1家」放宽通道补排名上限**：`seed.sector_relax_rank=5`（原无上限，
  会放排名 9~15 的中游板块进池，是最大输家桶）。
- **阶段 B 技术方案成文 HOLD**：`docs/WINRATE_PRIOR_PLAN_2026-08-24.md`——经验胜率先验
  需每桶 ≥30 样本，当前最大桶仅 9，暂不实现。
- **`review` 自动打印阶段 B 触发进度**（`followthrough.stage_b_bucket_stats`）：
  盘后回填时提示「最大桶样本 N/30（桶：…），还差 M 条达标」，样本够了即可开始实现。

### 修复（2026-08-24 共振评分逻辑 bug：乖离扣分反向 + 板块门槛未同步）

> 逐因子复盘发现共振评分有几处逻辑问题，直接拉低胜率：量价结构里「乖离>8%」扣分
> 在反向惩罚强势票（08-21 招金黄金乖离 24% 被扣分，永杉锂业乖离 -3.4% 反而 6/6 可买），
> 且与 veto 的乖离软否决（普通 15% / 龙头 25%）重复计数、阈值还更严。

- **移除量价结构「乖离>8%」扣分**（`preflight.score_nine`）：乖离是追高/回踩风险信号，
  统一交给 veto 把关，不再在量价结构里用更严的 8% 罚强势票。
- **板块门槛同步收紧**：`scoring.sector_rank_half 15→5`、`identity.sector_rank_strong 10→5`，
  与「只做前 5 板块」一致，中游板块（6~15）不再在共振和身份分里拿分。
- **板块强度 detail 文案动态化**：原来硬编码「≤8 / ≤15」，现在随配置显示，避免复盘被误导。

### 策略（2026-08-24 胜率归因：收紧板块排名门槛 + 技术指标入跟涨样本）

> 对 58 条已回填跟涨样本做逐维归因：总胜率 19%（T+1≥3% 计胜），最强信号是「板块排名」——
> 排名 1-3 板块 T+1 胜率 50%、T+5 均 +3.3%，而排名 6~15 板块 T+1 胜率仅 0~17%、T+5 均 -7%。
> 系统此前门槛太松，把中游板块（6~15 名）放进了候选池，是胜率的主要拖累。

- **板块排名门槛收紧**：`scoring.sector_rank_full 8→3`、`seed.seed_min_sector_rank 10→5`、
  `seed.sector_relax_rank_nozt 12→6`。只做最强前 3~5 板块，砍掉排名 6~15 的输家。
- **跟涨样本新增技术指标**（`followthrough.record_seed` + `runner._ft_entries`）：
  `bias_ma20` / `atr_pct` / `vol_ratio` / `turnover` / `intraday`，T+N 回填后即可做
  逐因子胜率归因（验证「乖离>8% 是否反向惩罚强势票」等假设），不再只靠总分干瞪眼。
- **跟涨样本补充归因字段**：`amount_yi`（成交额）/ `cap_yi`（市值）/ `rank_pct`
  （板块内排名占比）/ `odds`（盈亏比）/ `veto_labels`（否决原因），与上一批技术指标
  一起构成逐因子归因的完整字段集。
- **SEED 报告归档「宁缺毋滥」与「回填提醒」**（`runner.seed_plan`）：无可买时的
  「宁缺毋滥：今日无可买标的，不写计划」与「仍有 N 条历史跟涨样本未回填」这两条
  提示不再只留在控制台，一并写进 SEED_*.md 的扫描备注，随报告存档复盘。

### 策略（2026-08-24 大盘趋势：硬闸改为分级共振扣分 + 更稳的趋势定义）

> 08-21 的「大盘趋势硬闸」在贴线处会整个关死扫描（08-24 上证 3894.73 vs MA20 3895.10，
> 差 0.08% 即被判「趋势=0」），且与情绪/共振里的 MA20 信号重复计数、拦截日不落盘无法回测。
> 本次把「低于 MA20 不开」的二元硬闸，改成 9 分共振里「大盘趋势」维的分级扣分。

- **移除 `seed.require_market_uptrend` 硬闸**：弱势市不再让种子扫描短路成 EMPTY，扫描照常跑完、
  候选照常落盘（恢复拦截日的反事实数据积累）。
- **更稳的趋势定义（`scoring.market_trend_*`）**：不再单看「现价是否高于 MA20」，而是
  位置（点位 vs MA20，带 ±0.3% 缓冲）+ 方向（MA20 近 5 日斜率，±0.2% 阈值）合成 -2~+2 态势。
  缓冲消掉贴线震荡的来回翻转；斜率看中期成本重心方向，比当日涨跌稳。
- **分级共振扣分（`preflight.score_nine` 大盘趋势维 -1~+1，扣分封顶 -1）**：强趋势 +1、
  偏强/震荡 0、偏弱/弱势 -1。弱势市满分 6 = 门槛 6，完美票仍有机会过，而不是被锁死。
- **防守姿态不再因「MA20 下方」触发**（`sentiment.classify`）：MA20 位置统一交给共振
  「大盘趋势」维表达，避免同一信号在扣分和门槛上各罚一次（弱势市数学上无人能过）。
  防守姿态保留给「情绪分偏低 / 退潮 / 指数位置未知」等非重复原因。
- **指数快照新增 `ma20_bias_pct` / `ma20_slope_pct`**（`providers.base.index_double_route`），
  由 `indicators.ma_slope_pct` 计算，供共振评分与后续复盘使用。
- **移除 `block_new_eval_when_index_below_ma20` 会话门禁**：`run`/`eval` 不再因「上证 MA20
  下方」被硬拦（`check_session_start` / `check_code_gate`），弱势市同样只靠共振扣分表达。
  情绪侧的 `ma20_below_delta` 与「防守姿态 +1」仍保留，作为软信号。

### 策略（2026-08-21 大盘硬闸 + 防守门槛真收紧）

> 复盘 08-18 ~ 08-21：4 只「可买」选中后三天内普跌（大北农 T+1 -4.44%、陕西黑猫 -2.96%、
> 万和电气 -3.70%、润达医疗 -4.74%）。根因是共振评分里「大盘趋势」维权重太低、可被补偿，
> 导致下跌/震荡市里也能凑满 6 分逆势选股。

- **大盘趋势硬闸（`seed.require_market_uptrend`，默认 true）**：上证不在「涨 + 站上 MA20」时，种子扫描直接 EMPTY，不再逆势选股。拆解 4 只可买票六维得分，大盘趋势维全是 0/1——大盘不配合时是 β 在跌，共振分再高也扛不住
- **防守市龙头门槛不再抵消（`effective_threshold` 顺序调整）**：龙头 -1 先算、防守 +1 后算。之前防守 +1 被龙头 -1 抵消，龙头在防守市门槛仍是 6，等于防守没收紧（陕西黑猫情绪 47 防守市 6/6 仍被选）；现在防守市龙头也要 7 分卡

### 策略（2026-08-18 准入松绑：修复"9 天 0 可买"的结构性死锁）

> 复盘 08-08 ~ 08-18 的 30+ 次扫描与观察池/种子记录：从未产出过 1 只「可买」，
> 根因不是市场差，而是三处过滤器互相锁死。详见 `docs/STRATEGY_ADJUSTMENT_2026-08-18.md`。

- **R:R 门槛 3 → 2（`strategy.min_odds` + `scoring.sl_struct_min_odds`）**：止损硬顶 6% + 止盈上限 15% + 双边滑点 0.5% 的结构下，含滑点最大盈亏比 ≈2.1，3 恒不可达——观察池 12 只 odds 全部卡在 2.08~2.18，无一达标。降到 2 后由「止盈抬升」逻辑自动满足（需 tp≈14.4% ≤ 15% 上限）。同时修复连锁问题：「止损结构」维度因 `odds≥3` 恒为 0 分、有效满分从 9 掉到 8
- **分时高位/封顶否决改为仅在盘中交易时段生效**（`veto.skip_intraday_check_off_session`，默认 true）：盘后扫描时「现价≈当日最高」是强势股常态，分时位置≈1.0 会把龙头误判成「追高/封顶」——28 只候选里 71% 被此否决杀掉。跳过时在 `veto.intraday_skipped`/`intraday_note` 留痕；T+1 真实买入在 14:00–14:45 盘中重新评估，不受影响
- **龙头 MA20 乖离软否决阈值 20% → 25%（`strategy.veto_bias_leader_pct`）**：实证被 20%~26% 乖离否决的龙头 T+3/T+5 多数继续上涨（成都先导 +9.6%、博腾 +3.3%）
- **样本不足不再抬升通过门槛（`expectancy.insufficient_pass_bump` 1 → 0）**：当前零实盘样本，这个 +1 无依据地收紧本就过严的共振门槛
- **消息面维度中性化（`score_nine.has_news` 改为三态）**：自动种子扫描没有消息数据源，之前 `has_news` 默认 `False`，③ 消息面恒为 `0/1`，把有效满分从 9 悄悄压到 8、门槛却仍是 6。现在自动扫描 `has_news=None` → 该维 `0/0` 中性化（不计分、不占满分），满分诚实显示为 8；手动 `eval`/`run` 仍按「有/无催化」给 0/1

### 数据积累与运行日志（2026-08-18）

> 目标：**积累数据是第一位**，让每一次扫描/评估/调参都可回溯，据此迭代指标与阈值。

- **新增运行日志 `tea/core/logger.py`**：落到独立 `logs/` 目录（与 `data/` 结构化数据分开），标准库 `logging` + `TimedRotatingFileHandler` 按日切割（`logs/tea.log`，历史 `tea.log.YYYY-MM-DD`，保留 `logs.backup_days` 天）。日志子系统装配失败静默降级，不打断主流程。关键节点已接入：启动（配置/数据/日志路径）、`config set` 参数变更、每次种子扫描漏斗结果与跟涨样本去重数、盘后分时否决跳过
- **种子明细数据增强（`screener.candidate_row`）**：`scan_details_{date}.json` 的每只候选新增关键指标，供日后复盘迭代——技术/量价（`atr_pct`/`bias_ma20`/`vol_ratio`/`amount_yi`/`turnover`/`cap_yi`）、止损止盈（`sl_pct`/`tp_pct`/`odds`/`min_odds`）、**9 分共振逐维拆解 `scoring_dims`**、会话/否决留痕（`in_session`/`intraday_skipped`/`identity_flags`/`veto_labels`）。此前只有总分与裁决，看不清「哪一维在系统性拖分」
- **`scoring_dims` 增加 `detail` 字段**：把「消息面 0/0（中性化）」「板块强度 stale 归零」等特殊状态的原因一并落进 `scan_details_*.json`，复盘时能一眼区分「数据缺失」还是「真没分」
- **修复 `phase1.py` 未同步 `in_session`**：`run` 交互流现在同样按 `s.tm.in_session()` 判定，`--any-time` 盘后执行时不再误用失真分时否决（与 `eval`/种子扫描行为一致）
- **历史跟涨样本去重（`followthrough.dedupe_records`）**：一次性清理已落盘的重复记录（本次实测 `data/seed_records.jsonl` 84 → 32 条，去掉 52 条重复）；`record_seed` 防新、`dedupe_records` 清旧，两者配套
- **种子扫描收尾提示未回填的 T+1 样本**（新增 `followthrough.pending_backfill`）：若 `seed_records.jsonl` 里仍有历史记录 `result=null`，扫描结束屏上提示 `跑 tea review 补齐`，避免跟涨胜率模块长期零样本（当前 32 条记录全部未回填，胜率一直在跑默认值）
- **打包 spec 补齐隐式导入**：`tea.core.logger` 与 `tea.reporting.retrospective` 加入 `packaging/tea.spec`（此前后者缺失导致打包后复盘命令 ImportError）

### 数据可靠性（2026-08-18）

> **板块排名要准确——它直接影响选股**。旧逻辑里板块排名「磁盘缓存优先于实时」，
> 24h 内都不重拉，等于拿昨天的板块排序选今天的股。已改为「实时优先、失败才回退」。

- **板块排名改为实时优先（`Market.get_sector_ranking`）**：内存缓存（600s）过期后
  **先实时取数**，实时失败才回退 24h 磁盘兜底并标注 `Market.sector_stale`（旧逻辑是
  磁盘缓存优先，24h 内不重拉）。种子扫描第 1 步据此给出醒目告警：
  「⚠️ 板块排名实时取数失败，正使用磁盘兜底缓存（可能是昨日数据，选股方向仅供参考）」；
  天气屏同样提示「⚠️ 板块排名为磁盘兜底缓存」
  - 自测新增「板块排名回退缓存且标注 sector_stale」断言，自测 404 → **405**

> 问题：`涨跌家数`（clist）与 `涨停池`（ztpool）是东财独家、无备源，且东财会
> 间歇性 `RemoteDisconnected`（实测约 1/3 概率失败），导致天气里出现
> 「涨跌比 — / 涨停 — 家」，情绪分随之失真。

- **涨跌家数 / 涨停池新增磁盘兜底缓存**：实时取数失败时回退最近一次成功值
  （`data/.tea_breadth_cache.json` / `.tea_ztpool_cache.json`，保留时长
  `market.breadth_disk_cache_hours` / `ztpool_disk_cache_hours`，默认 6h），
  并标注 `stale=True`；天气屏显示「· 涨跌家数/涨停池为缓存回退值（实时取数失败，
  非实时）」。兜底不再写 `error` 字段，避免被误报成「数据缺口」
  - 实现：`Market.get_breadth` / `get_limit_up_stats` 拆出实时计算与兜底两层，
    复用通用 `_kv_disk_load` / `_kv_disk_save`；失败路径记 `logs/tea.log` 警告
  - 自测新增「数据层 · 涨跌家数/涨停池磁盘兜底」3 项断言（回退命中 + stale 标注 +
    不含 error），自测 401 → **404**

- **板块排名 / 涨跌家数重试覆盖整条 CDN 节点池**（新增 `market.sector_retries` /
  `breadth_retries`，默认 3）：两者都是东财独家、无备源，全局 `retries=2` 只轮
  `cdn_hosts_quote` 三个节点里的前两个，第三个（常是可用的 `push2delay`）根本
  试不到就回退磁盘兜底。现在这两个关键单源取数点单独抬到 3 次重试、覆盖全节点池
  后再回退——治本，减少「回退昨天数据」的发生
  - 自测新增「板块排名/涨跌家数重试覆盖全节点池 + 作用域用完即还原」断言

- **东财报价 / K 线重试覆盖各自节点池**（新增 `market.quote_retries`=3 /
  `kline_retries`=4）：报价池 3 个、K 线池 4 个节点，全局 `retries=2` 只试前两个，
  健康节点排在后面就漏掉、被迫切到腾讯等备源——日志里「`1.push2his` 网络抖动 →
  改用腾讯」就是这么来的。现在主源内覆盖整条池，减少无谓降级
  - 实现：`IDataProvider.retry_scope`（与 `timeout_scope` 对称，用完即还原），
    东财 `fetch_quote` / `_point_chg` / `fetch_klines` 分别套用 quote/kline 重试
  - 自测新增「东财 K 线/报价重试覆盖节点池 + 作用域还原」断言

### 策略

- **涨幅窗口下限动态化**：根据当日最强板块涨幅自适应（≥5% → 3.0%，4~5% → 2.5%，<4% → 2.0%），弱市不再系统性空仓。硬地板 2.0%（再低 R:R 撑不到 ≥3）；影响严格档 / 萌芽窗口 / 热点降级档，动量档（5.0%）不变，严格档上限 5.5% 不变（窗口只变宽）
  - 板块排序里的「温和票结构分」与系统分的「涨幅贴合」同步跟随动态下限，否则窗口放宽了但打分还按 3% 数，白放宽
  - 前夕观察上限跟着收（下限降到 2.5% 时前夕窗为 1%~2.5%），避开与严格窗重叠——否则同一只票两边都进，「等它涨入严格窗」的触发条件当场自相矛盾
  - 扫描输出 / SEED 报告新增一行「动态窗口：下限 2.5%（最强板块 +4.30%，标准 3.0% → 下调至 2.5%）」，当前用的是什么阈值一眼可见
  - 新增配置：`seed.dyn_min_chg_enabled`（置 false 回到固定下限）/ `dyn_min_chg_floor` / `dyn_strong_sector_chg` / `dyn_weak_sector_chg`

### 体验

- **数字菜单从 20 项平铺改为按时段推荐 4 项**：同一时刻真正有意义的操作不超过四个，平铺 20 项只会让人每次重新读一遍。`m` 展开全部（按道法 / 准入 / 计划 / 持仓 / 复盘 / 工具 分六组），`c` 收起。**编号不重排**，1-20 任何时候都能直接输，没列出来的也能输
- 菜单里直接回车不再报「无此选项」，只重印菜单
- **切数据源时屏上说清「谁挂了、改用谁」**：原先只有一句含糊的「⏳ 网络抖动，正在重试...」刷屏，用户既不知道是哪一步在抖，也不知道降级到底有没有生效。现在降级链每次切源直接报 `⏳ 东财行情失败（请求失败(2次): ...），改用 腾讯 数据源...`；链尾无备源时报「已无备用数据源」。通用重试提示保留但节流放宽到 5s（`market.retry_notice_gap_sec`），不再和切源提示叠着刷
- 取数收尾新增一行数据源命中账：`网络请求 68 次｜东财 45｜腾讯 18｜失败 0`（种子扫描 / 天气 / 筛选共用），降级链是否真的在干活一眼可见
- **计划绑定相关提示补全股票名**：光一串代码认不出是哪只票。种子扫描「宁缺毋滥」分支的旧计划提醒、代码门禁 `check_code_gate` 的「计划绑定」拦截文案、`gates.format_status` 的今日状态行，全部从裸代码列表改为「代码 名称」（计划里没记名字时回退到裸代码）；`status.plan_codes` 与 `planned_codes()` 语义保持不变，机器字段仍是纯代码

### 修复

- **计划写入按 code 幂等（`plan.active_codes_equal` + `runner.seed_plan`）**：同日多次种子扫描产出同一批 code 时，不再重复写 `trade_plan.json` 和 `plan.write` 记录（实测 12:49/12:54/13:00 同一计划 002385 写了三次）。现在检测「execute_date + 有效 code 集合」一致则跳过重写并提示，accumulator 里同一计划不再被重复计数
- **种子扫描第 3 步用实时行情复检涨幅窗口**：第 2 步按板块成分股的批量快照涨幅（clist）筛入涨幅窗，可能比实时行情慢一拍，导致「显示 6.77% 实际已涨到 7.6% 却还当温和票」。现在 `veto_filter` 拿到实时报价后按当初筛入的窗口下/上限再核一次，移出窗口的候选标记为「涨幅移出窗口」、不再作为种子输出（`screen_tier` 在候选里记 `win_min`/`win_max`）
- **「动态窗口」与「跳过严格档」统一用同一个最强板块涨幅**：原先动态窗口按排名表首位（`strongest_sector_chg`，实测 13.56%），跳过严格档却按 TOP3 入池后的最大值（`max_sector_chg`，实测 11.10%），两条备注印出两个不同的「最强板块涨幅」。现在两者都用 `strongest_sector_chg`（排名表首位），口径一致
- **跟涨样本按 (date, code) 去重（`followthrough.record_seed`）**：`seed-plan` 一天跑多次时同一标的被重复 append（84 条记录去重后仅 32 个唯一），导致 T+1 样本翻倍、跟涨胜率失真。现在只落首条，返回 `{"added": n, "skipped": m}` 并在控制台提示去重数量
- **东财 `clist` 接口硬限 100 行/页，配置里却写着 `pz=6000`**：`pz` 填多大都不报错，只是默默地只回 100 行；叠上 `po=1&fid=f3`（按涨幅降序），拿到的是涨幅榜前 100 名——数据看着完整，实际是极端偏样本。现在按接口自报的 `data.total` 真正翻页：
  - `get_breadth` 的涨跌比不再结构性恒为 100%。全市场五千多只按 100 行/页翻完要 56 次请求，与防封目标冲突；但列表按涨幅降序，「涨幅 > 阈值」是一段前缀，于是二分定位到跨界那一页、页内线性数一遍，得到**精确值**。实测 9 次请求拿到 5545 只的确切涨跌家数。返回值新增 `exact` 标记探测预算是否够用
  - `get_sector_members` 翻完整个板块。之前只取前 100 名对种子扫描是致命的——它要找的正是 3.0~5.5% 的温和票，而这些票全在涨幅榜前 100 名之外，等于在一个结构性不含目标的样本里搜索
  - `get_sector_ranking` 不再停在 300 个板块（3 页 × 100），实测 5 次请求拿全 496 个
  - 板块磁盘缓存加版本号：翻页修好前落盘的缓存只有 300 个板块，24h 内会被读回来把修复盖住
  - 请求量实测：涨跌家数 9 次 + 板块排名 5 次 + 前 30 板块成分 31 次 = 45 次（前 30 个板块里只有 1 个超过 100 只，所以成分部分只多花一次请求）
- **非交易日情绪输入日期不一致**：指数 / 板块 / 涨跌比都会自动给上一个交易日，只有按日期取的涨停池返回 0，于是出现「涨停 0 家」旁边坐着「前5板块均涨 10.31%」——情绪公式拿这两个不同日子的数做加减，算出来的周期是假的。`get_limit_up_stats` 未指定日期时按交易日往前回退（默认最多 3 天），并在屏上标出它到底是哪一天的数据
- **涨停池的 `ut` 令牌一直是错的**：接口回 `rc=205, data=null`，于是涨停家数永远是 0、最高连板永远是 0。已换成正确令牌（实测拿到涨停 62 家 / 最高 6 板）
- **取数失败不能假装成「今天没有涨停」**：这是上一条真正危险的地方。当成 0 后，情绪公式会拿「0 板」扣 10 分，还可能触发冰点降仓把半仓乘数强制打到 0.25——一个网络 / 令牌问题悄悄变成了减仓指令。现在 `rc非0` 返回 `None` 并标 `ok=False`，情绪公式遇 `None` 直接跳过该项，屏上另行提示数据缺口
- 涨停家数改用接口自报的 `tc`：`pool` 被 `pagesize` 截断时 `len(pool)` 会偏小
- **指数取数失败时屏上写着「上证在 MA20 下方」**，实际是不知道。拦住新开这个方向对一个纪律引擎是对的（保守），但话得说准：现在显示「未知」并注明原因。更进一步——点位（走 `quote` 池，有 `push2delay` 兜底）与 MA20（走 `push2his` 那族，常被单独封）本是两路，旧代码 K 线一挂就把已到手的点位一起丢，屏上「上证 —（—）」。现在只丢 MA20、保留点位（`上证 3806 … MA20 — → 未知`）；且「不知道 MA20」不再被退潮判据当成「在 MA20 下方」编出假退潮（周期照分走主升 / 发酵，姿态仍落防守）
- **「姿态=防守」的理由把三个候选原因一股脑印出来**，于是出现「情绪分 84」旁边写着「情绪分偏低」。现在只列真正触发的那几条并带上具体数值
- **本机配了翻墙代理时东财请求全崩（`ProxyError`）**：东财是国内站，但 shell 里为 GitHub 等设的 `http_proxy` / `https_proxy` 会被 requests 默认读走，把域名请求硬塞进代理，代理一连不上就 `Unable to connect to proxy` 全线失败——屏上表现为指数 / 板块 / 涨跌比全是「数据缺口」。现在默认 `trust_env=False` 直连，忽略环境里的代理；境外等真需要代理取数的场景置 `market.use_env_proxy: true` 或直接配 `proxy_pool`
- 取数失败时涨跌比括号里漏印成字面量 `涨 None / 跌 None`，现在渲染为 `涨 — / 跌 —`
- **CDN 节点按个坑时，坏节点会拖垮整次取数**：实测 `push2` / `push2his` 某些节点直接 `RemoteDisconnected`，而 `push2delay` 这类节点是通的。旧轮换每次 `random.choice`，一次操作里多个请求各自随机挑节点，可能三次重试全撑在同一个坏节点上，而且不记事——上一个请求刚确认 push2 死了，下一个又去撞（这就是为何同一节点池下板块成功而涨跌比失败）。现在：同一次 `get_json` 的多次重试走不同节点；连接失败的节点记入会话黑名单，后续请求先活后死排序（成功则自动复活，整池全坏仍会把每个节点都试一遍）。kline 节点池从 2 个扩到 4 个（`push2his` / `1` / `2` / `7`）。单次请求的尝试次数取 `max(retries, 节点数)`——老配置把 `retries` 钉在 3、而 kline 池有 4 个节点时，也保证每个节点都轮得到；且加载配置时始终把 DEFAULTS 里的 CDN 节点并进用户配置（`save()` 落的是全量配置，否则新增的官方镜像节点进不来已有配置）。实测：在 `push2` / `push2his` 均被封的环境下，默认配置无需手工指定主机即可拿全指数 / 涨跌比 / 板块 / 涨停池
- 行情与 K 线接口从 `http://` 改为 `https://`（`clist` / `ztpool` 本来就是 https）
- **东财 `fltt=2` 返回值被错误除以 100**：`fltt=2` 返回的已经是最终浮点数（放大 100 倍的整数是 `fltt=1`）。`_parse_quote` 与 `get_index` 还在除 100，导致上证 3832 点显示成 38.32 点；而 MA20 来自 K 线（真实点位），于是「上证在 MA20 下方 → 禁止新开」这条门禁**永久锁死**。个股侧同样受影响（价格 / 涨幅 / 量比 / 换手率全部小 100 倍）
- 11:30–13:00 的时段名从「盘后」改为「午间休市」
- **Windows 上输出被重定向时程序直接崩溃**：输出不是直连控制台（管道、`> file`、CI 日志）时 Python 用 ANSI 代码页（cp1252 / cp936），而本程序文案全是中文，第一句 print 就 `UnicodeEncodeError`。现在入口处统一把 stdout / stderr 切到 UTF-8
- `.gitignore` 的 `data/` 未锁定根目录，连 `tea/data/`（行情数据层包）一起忽略掉了；已改为 `/data/` 与 `/reports/`
- **5 源降级默认没启用，等于白实现**：`market.data_sources` 默认只有 `["eastmoney"]`，降级链上就一家，东财一抖整轮取数照样全崩——用户看到的仍是满屏「网络抖动」加二十几个板块「拉取失败」。现在默认五家全开（东财 / 腾讯 / 新浪 / 网易 / 凤凰）
  - 单源时代已落盘的老配置会在下一次启动时**自动升级一次**（屏上一次性提示；标记写在 `meta.multisource_migrated`，重复启动不再动手）。用户显式配了别的组合（例如只留东财+腾讯）则原样尊重，只打标记不覆盖
  - 同源重试 `market.retries` 4 → **2**：后面还有四家备源，在第一家身上死磕 4 次 × ~3s 只是把降级往后拖十几秒
  - 板块成分（东财独家、扫一轮要问 30 次）的重试单独压到 `market.member_retries: 2`，用完即还原，不影响其余取数点的全局重试预算

### 工程

- **参数变更留痕（`accumulator.record_param`）**：`tea config set <key> <value>` 现在把 `old → new` 写进 `accumulator.jsonl`（`param_change`），与扫描/评估/交易同一条追溯链，事后可回答「何时改了哪个阈值、改前是多少」
- **R:R / 分时判定关键节点留痕**：`compute_levels` 返回 `min_odds`；`veto.check` 返回 `intraday_skipped`/`intraday_note`；`preflight.evaluate` 返回 `in_session`——这些字段随 scan_details / 观察池快照 / 种子记录落盘，便于复盘「为什么被拒/被放行」
- 自测 245 → **275 项**。新增：老配置迁移到 5 源 / 用户显式组合不被覆盖 / 迁移幂等（含已迁移过又手工改回单源时不再二次升级）、`load_config` 端到端只提示一次且确实落盘、配置文件不存在时不凭空创建、降级链命中计数与切源提示文案、切源只跳到「真的实现了该方法」的下一家、`member_retries` 作用域生效且用完还原
- 自测 97 → **163 项**。新增：用东财真实 `fltt=2` payload 验证行情解析量级（含尺度无关的 `(price/pre_close-1)*100 == chg_pct` 自洽断言）、模拟 clist 硬限行为验证精确涨跌家数与板块成分不被截断、涨停池按交易日回退、坏令牌不被当成「涨停 0 家」且不触发冰点降仓、CDN 节点故障转移（坏节点不拖垮取数、会话内记住坏节点、整池全坏仍逐个试）、K 线单挂时保留指数点位且 MA20 未知不谎报退潮、菜单分组与 MENU 完全对齐、各时段建议 1-4 条且均合法
  - 之前的 97 项全走 `FakeMarket`，绕开了 `_parse_quote` / `get_index` / `get_breadth` / `get_sector_members`——数据层一直没被任何断言跑过，上面那些 bug 就是这么漏出去的
  - 新的数据层假接口刻意复刻真接口的坑：无论 `pz` 填多少都只回 100 行，但 `total` 报真值。断言里的数字（全市场 5545 只、机械设备 613 只成分、437 个板块）都是实测值
- 自测最终：**335 项**（含 5 源降级 / retries 硬上限 / 板块缓存 schema 版本等新增断言）
- CI 新增「非 UTF-8 输出环境」断言，用 `PYTHONIOENCODING` 在任意平台复现 Windows 的编码条件，不必等 Windows 跟尾才发现回归
- `run.bat` 开头 `chcp 65001`，使中文错误提示在 cmd 里不乱码

### 配置

- 默认值变更：`strategy.min_odds` 3 → 2、`scoring.sl_struct_min_odds` 3.0 → 2.0、`strategy.veto_bias_leader_pct` 20 → 25、`expectancy.insufficient_pass_bump` 1 → 0；新增 `veto.skip_intraday_check_off_session: true`。已落盘的 `tea_config.json` 需手动同步（`save()` 写全量配置，DEFAULTS 不覆盖既有值）
- 移除 `sector_pages` / `sector_page_size` / `member_page_size` / `breadth_page_size`（它们描述的是接口不存在的能力）；新增 `sector_max_pages: 12` / `member_max_pages: 10` / `breadth_max_probes: 24` / `ztpool_fallback_days: 3`；再新增 `use_env_proxy: false`（是否让请求走 shell 环境里的代理，国内站默认关）、`member_retries: 2`（板块成分单独的重试预算）。默认值变更：`data_sources` 单源 → 五源全开、`retries` 4 → 2、`retry_notice_gap_sec` 2.5 → 5.0。参数总数 362 → **410**

## [1.0.0] - 2026-08-02

首个完整版本。按"道-法-术"三层框架从零实现 A 股交易准入引擎。

### 道（市场天气）

- 情绪评分：基准 50 / 上限 100，六项加减（涨跌比、最高连板、上证 MA20 位置与涨跌幅、热点板块数、前 5 板块均涨）
- 周期判定：冰点 / 修复 / 发酵 / 主升 / 高潮 / 退潮
- 交易姿态：空仓 / 防守 / 进攻，含"高潮易分歧"禁新开
- 冰点降仓：最高连板 ≤3 且涨跌比 <35% → 半仓乘数强制 ×0.25
- 交易窗口与阶段判定（`timing`），情绪快照 120s 内存缓存

### 法（纪律门禁）

- 三道门禁：会话门禁、代码门禁、买入门禁
- 计划绑定作为核心纪律：今日无有效计划或代码不在计划内 → 禁止评估与买入
- 买入时间窗 14:00–14:45；单日最多新开 1 笔 / 评估 5 次 / 同票复筛 2 次
- 上证 MA20 下方禁止评估新开；连亏冷却
- 动态门槛：基准 6，杂毛 +1、样本不足 +1、防守姿态 +1，种子龙头 -1（下限 6）
- 仓位 3/7 灰度确认制，半仓乘数 = 情绪 × 期望值 × 跟涨，夹紧 [0.25, 1.0]
- 期望值 E[R] = p̂ × odds − (1 − p̂)，样本不足时按默认胜率 0.40 并标记
- 计划复核：逐项对比快照，任一变动整单作废

### 术（个股评分）

- 9 分共振六维：板块强度 2 / 大盘趋势 1 / 消息面 1 / 市值区间 2 / 量价结构 2 / 止损结构 1
- 身份判定 100 分 6 维，龙头 ≥70 / 跟风 ≥45 / 杂毛 <45，≥2 个负面 flag 强制降为杂毛
- 热点板块相对跟涨：相对系数 0.42~0.90 → +18，板块前排额外 +12
- ATR(14) 优先的止损止盈：止损 clamp(ATR%×1.5, 2%, 8%) 硬顶 6%，止盈 min(ATR%×3, 15%)
- 含滑点 R:R（买入 +0.5% / 止损 -0.5%），要求 ≥3，不足则抬止盈反推
- VETO 一票否决：硬否决 6 项 + 软否决 4 项，20cm 板阈值等比 ×2

### 扫描与计划

- 种子扫描四步流：板块综合排序 → 三档涨幅窗筛选 → VETO → 预审快照与三档输出
- 观察池闭环：纳入 / 剔除 / 回踩就绪
- 落选追溯：`seed_trace.jsonl` + `SEED_TRACE.md`，记录每一步淘汰了谁
- T+1 跟涨经验回填，按阶段×档位×轨道聚合胜率
- 当日累积（回答"为什么今天没交易"）、统计归因、周复盘

### 工程

- CLI：23 个子命令 + 20 项数字菜单，交易逻辑全部下沉，`cli.py` 不含策略
- 一键启动：`run.sh`（macOS / Linux）与 `run.bat`（Windows，可双击），自动挑 3.8+ 解释器、把数据目录锁在仓库根、参数原样透传；CI 在三个平台各启动一次
- 分层：行情数据层拆为 `tea/data/`（indicators → cache → fetcher → market，依赖单向无回边），四阶段流程拆为 `tea/phases/`（results / prompt / session / phase1~4）；两个子包的 `__init__.py` 只做再导出
- 配置：363 个可调参数，JSON 深合并，缺项自动回落默认值，新增参数自动生效
- 行情：东方财富 push2 系列接口，随机 UA/Referer、请求延时、代理池、指数退避重试、板块成分多级缓存
- 三路数据并行采集，单路失败降级不影响整体
- 所有文件写入原子化（先写 `.tmp` 再 `rename`）
- 运行时零第三方依赖；`requests` 为可选加速项，缺失时回退 `urllib`
- 离线自测 97 项断言，全部按规格独立重算后比对，沙箱运行不触碰真实数据

### 已知限制

- 离线自测不触网，**东财接口的真实连通性与字段映射尚未经实盘验证**
- 观察池闭环与防封策略没有自动断言覆盖

## [0.x] - 2025-03-31

内部开发里程碑。

### 种子扫描覆盖范围放宽

- 开启科创板扫描 `permissions.star` → `true`
- 扩大板块初筛池 `seed.sector_scan_topn` 30 → 40
- 提高预审候选上限 `seed.candidate_fetch_cap` 60 → 80
- 增加成分股拉取页数 `market.member_max_pages` 10 → 15

### 新增每日扫描明细日志

- `tea/screening/screener.py` 新增 `_write_scan_log` 方法
- `seed_scan` 结束后将完整候选明细（含裁决、硬否决原因、各项分数）写入 `data/scan_details_{date}.json`
- 写入失败不会中断主流程，日志文件存储于数据目录，供周末复盘使用

### 项目开发规则文档

- 新增 `.github/PULL_REQUEST_TEMPLATE.md` 与 `CONTRIBUTING.md`
- 明确分支管理、单元测试要求以及合并准则

### 变更文件清单

- `tea/config/config_store.py`
- `tea/screening/screener.py`
- `.github/CONTRIBUTING.md`

### 影响评估

- 所有配置改动仅影响默认值，已存在的 `tea_config.json` 不受影响
- 日志文件每日生成，磁盘占用极小
- 科创板权限开启后可能增加候选股，但后续 VETO 与身份分过滤仍会保证质量
