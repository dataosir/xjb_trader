查看每个 `tea` 的详细信息，判断哪个是源码（应包含 `__init__.py`、`config` 等内容），哪个是多余的。

### 步骤 2：删除多余的目录
- 如果多的那个是空目录或纯拷贝，直接删除：
  ```bash
  rm -rf path/to/duplicate_tea
  ```
- 如果两个目录都有重要文件且不同，先合并内容到主 `tea/`，再删除。

### 步骤 3：清理 Git 缓存中的大小写冲突
如果是因为大小写导致的，需要让 Git 重新识别：
